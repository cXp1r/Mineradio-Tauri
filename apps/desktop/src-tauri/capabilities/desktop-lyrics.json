{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-lyrics",
  "description": "Desktop lyrics overlay window can receive core window and event permissions only.",
  "windows": ["desktop-lyrics"],
  "permissions": [
    "core:window:default",
    "core:event:default",
    "core:webview:default",
    "core:app:default"
  ]
}
