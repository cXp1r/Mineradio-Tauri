{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Mineradio runtime shell capabilities for main-window system commands. Renderer dialog plugin commands are not exposed; JSON import/export is owned by Rust commands.",
  "windows": ["main"],
  "permissions": [
    "core:window:default",
    "core:event:default",
    "core:webview:default",
    "core:app:default"
  ]
}
