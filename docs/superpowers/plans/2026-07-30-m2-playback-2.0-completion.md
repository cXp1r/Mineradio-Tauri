# M2 Playback 2.0 完成实施计划

**设计：** `docs/superpowers/specs/2026-07-30-m2-playback-2.0-completion-design.md`

**基线：** `92a6447`

**当前状态：** Completed（Code Complete / Automated Verification Complete，2026-07-30）

**TDD 范围：** 只对 Audio owner、handoff authority、fade/crossfade、恢复预算、output routing 和 deterministic diagnostics 使用严格 RED → GREEN；机械 React/Visual wiring 使用 characterization、architecture guard、typecheck 与 build。

**范围冻结：** 保留当前 Bun Sidecar API；不修改 `sidecars/api/**`、`packages/shared/**`、`sidecar-client.ts`、Rust supervisor、Sidecar build/Cargo/`externalBin`，不接入开发中的 `MineRadio-api`。

## Completion map

| Tracer | 用户可见结果 | 核心证据 |
| --- | --- | --- |
| A | pending 不会抢占当前可听歌曲 | exact handle/generation + owner tests |
| B | 相邻同专辑预载、gapless/crossfade 与失败降级 | handoff policy + A/B deck tests |
| C | 双 deck 共用 Audio Graph，视觉不断音 | graph ownership + readonly frame source tests |
| D | play/stall/静音 Graph/fade 恢复有上限 | fake clock budget tests |
| E | primary + 最多四 mirrors + Virtual Output Bridge | routing/preference/devicechange tests |
| F | UI、文档、预算与冻结门禁收口 | architecture/perf/full gates |

## Task 0：冻结基线并保留 foundation

**只验证，不重做：**

- `playback-state-machine.ts`；
- `playback-session-coordinator.ts`；
- 单调 `playbackIntentId`；
- source-bound `PlayerController` event；
- existing URL-age/long-pause/quality/media-error policy；
- M9 Application Ports 与 Legacy Sidecar Adapter。

- [x] 运行 foundation characterization：

```powershell
bun test --parallel=1 apps/web/src/features/playback apps/web/src/audio/player-controller.test.ts apps/web/src/stores/playback-store.test.ts
bun test --parallel=1 scripts/architecture/playback-session-boundary.test.ts scripts/architecture/m9-api-readiness-boundary.test.ts
```

- [x] 记录当前测试数量和 M9 frozen object guard 结果。
- [x] 确认本阶段没有新 route、DTO、Tauri command 或 Cargo dependency。

## Task 1：Handoff policy、typed preference 与 compare-and-commit

**Files：**

- Create: `apps/web/src/features/playback/playback-handoff-policy.ts`
- Create: `apps/web/src/features/playback/playback-handoff-policy.test.ts`
- Modify: `apps/web/src/stores/playback-store.ts`
- Modify: `apps/web/src/stores/playback-store.test.ts`
- Modify: `apps/web/src/preferences/keys.ts`
- Modify: `apps/web/src/preferences/keys.test.ts`
- Modify: `apps/web/src/preferences/legacy-preferences.ts`
- Modify: `apps/web/src/preferences/legacy-preferences.test.ts`
- Modify: `apps/desktop/src-tauri/src/db.rs`（只扩充既有 preference allowlist/schema；不触碰 Sidecar）

### 1.1 RED：保守 Album Gapless Candidate

- [x] 证明 provider/id/album/cover 任一缺失均不 eligible。
- [x] 证明 album 使用 NFKC、trim、lower-case 和折叠空白；cover trim 后必须完全相同。
- [x] 证明 `single`、`shuffle`、非相邻 index、queue fingerprint 变化与新 intent 均拒绝。
- [x] 证明 8.5s preload、1.05s preroll、720ms equal-power、360ms min、56ms attack、180ms adopt 为纯确定性输出。

### 1.2 GREEN：不可变 policy authority

- [x] 实现 `resolveAlbumGaplessCandidate()`、`authorizeAlbumGaplessPreload()`、`claimAlbumGaplessPreloadCommit()`、`claimAlbumGaplessAdvance()` 和 invalidate/dispose。
- [x] authority 绑定 generation、session、intent、candidate、current track 和 queue fingerprint。
- [x] ended 与 handoff advance 只能有一个 winner；重复 claim 返回 false。

### 1.3 RED → GREEN：store commit 与 preference

- [x] `commitPreparedHandoff()` 只对 exact outgoing intent、严格相邻 candidate 原子成功一次，并生成新 `playbackIntentId`。
- [x] 失败不改变 queue、current track、position 或 intent。
- [x] `PlaybackAudioPreference` clamp fade 为 0–3000ms，mirror 去重/排除 primary/最多四个，bridge 无 deviceId 时自动 disabled。
- [x] 旧 fade/output storage 只通过既有 migration Adapter 迁入 typed preference；损坏值回 defaults。

验证：

```powershell
bun test --parallel=1 apps/web/src/features/playback/playback-handoff-policy.test.ts apps/web/src/stores/playback-store.test.ts apps/web/src/preferences
```

建议提交：`feat(playback): define prepared handoff policy`

## Task 2：建立 PlaybackAudioRuntime deep Module

**Files：**

- Create: `apps/web/src/audio/playback-audio-runtime.ts`
- Create: `apps/web/src/audio/playback-audio-runtime.test.ts`
- Modify: `apps/web/src/audio/player-controller.ts`
- Modify: `apps/web/src/audio/player-controller.test.ts`
- Modify: `apps/web/src/features/playback/PlaybackRuntimeHost.tsx`
- Modify: `apps/web/src/features/playback/PlaybackRuntimeHost.test.tsx`

### 2.1 RED：owner/generation contract

- [x] 初次 load 只创建 pending，pending `play()` 成功后才成为 committed。
- [x] A committed 时 B prepare/load 不改变 active element、position、duration 或事件 authority。
- [x] `playPrepared()` reject、timeout、stale generation、伪造 handle 和 aborted handle 都保留 A committed。
- [x] B commit 后 A 成为 retiring/idle；旧 A native events 全被抑制。
- [x] 任意时刻 committed ≤1、pending ≤1、handoff ≤1、deck exactly 2。
- [x] dispose 幂等，并使全部 handle/event/timer 失效。

### 2.2 GREEN：A/B deck 和 compatibility Adapter

- [x] Runtime 创建两个 lifetime deck，并为每次 source load 签发 exact context。
- [x] `PreparedPlaybackHandle.abort()` 只释放它拥有的 pending source。
- [x] `PlayerController` 保留现有 Interface 与 source-bound payload，内部委托 Runtime。
- [x] `PlaybackRuntimeHost` 创建/订阅/dispose 一个 Runtime，不直接管理第二套 deck/listener。
- [x] 增加 `ownerchange`、`playing`、`waiting`、`stalled`、`canplay` typed event；只有 owner event进入 application callbacks。

验证：

```powershell
bun test --parallel=1 apps/web/src/audio/playback-audio-runtime.test.ts apps/web/src/audio/player-controller.test.ts apps/web/src/features/playback/PlaybackRuntimeHost.test.tsx
```

建议提交：`refactor(playback): own audio through dual-deck runtime`

## Task 3：接入 session runtime 与 prepared adoption

**Files：**

- Modify: `apps/web/src/features/playback/playback-state-machine.ts`
- Modify: `apps/web/src/features/playback/playback-state-machine.test.ts`
- Modify: `apps/web/src/features/playback/playback-session-coordinator.ts`
- Modify: `apps/web/src/features/playback/playback-session-coordinator.test.ts`
- Modify: `apps/web/src/features/playback/usePlaybackSessionRuntime.ts`
- Modify: `apps/web/src/features/playback/usePlaybackSessionRuntime.test.tsx`
- Modify: `apps/web/src/app/App.tsx`（只做依赖装配）

### 3.1 RED：physical owner 与 application session 对齐

- [x] pending native `play` 不能把 state machine 推进 `playing`。
- [x] exact `ownerchange(loadHandle, source)` 才能提交 current load。
- [x] ownerchange 的 handle、generation 或 source URL 任一不匹配时拒绝。
- [x] prepared owner adoption 不再次请求 URL、不重新 `load()`、不重复 `play()`。
- [x] owner commit 后 queue/current track/lyrics/position 只 compare-and-commit 一次。
- [x] simultaneous ended 与 handoff completion 只触发一次 next intent。

### 3.2 GREEN：one-shot adoption seam

- [x] session runtime 把已 resolve 的 opaque source 交给 Runtime，不接触 deck/gain/Graph。
- [x] prepared result 携带当前 source/load context，供新 session 一次性 adopt。
- [x] manual switch、quality reload、source switch、recovery 和 stop 都取消旧 prepared handle。
- [x] 保留现有 trial/local/lyrics/beatmap/error/toast 文案与 Sidecar method 调用。

验证：

```powershell
bun test --parallel=1 apps/web/src/features/playback apps/web/src/stores/playback-store.test.ts apps/web/src/app/App.test.tsx
```

建议提交：`feat(playback): commit prepared audio owners`

## Task 4：Gapless、equal-power crossfade 与 fade cancellation

**Files：**

- Modify: `apps/web/src/audio/playback-audio-runtime.ts`
- Modify: `apps/web/src/audio/playback-audio-runtime.test.ts`
- Modify: `apps/web/src/features/playback/usePlaybackSessionRuntime.ts`
- Modify: `apps/web/src/features/playback/usePlaybackSessionRuntime.test.tsx`

### 4.1 RED：transition math 与失败降级

- [x] 720ms equal-power 曲线在 `t=0/0.5/1` 的 gain 确定且有界。
- [x] 尾部不足时 mix 不低于 360ms；incoming attack 56ms，adopt slew 180ms。
- [x] crossfade disabled 时只预载，不产生重叠可听 output。
- [x] incoming `play()` 成功前 outgoing gain 不被永久改变。
- [x] incoming reject/stale 时恢复 outgoing，并保留普通 ended/next fallback。
- [x] 新 intent、stop、dispose、hidden settle 取消旧 frame/timer，旧 callback 不写新 owner。

### 4.2 GREEN：统一 handoff

- [x] remaining ≤8.5s 时准备 idle deck，≤1.05s 时只允许 0 gain/muted preroll。
- [x] gapless 与 crossfade 共享同一 prepared handle 和 authority。
- [x] 普通 fade defaults 为 460/420ms，范围 0–3000ms；crossfade 不叠加普通 fade。
- [x] hidden/deep background 立即收敛目标 gain。
- [x] audibility probes 为 520/1400/3200ms，且每 generation/阶段最多一次。

验证：

```powershell
bun test --parallel=1 apps/web/src/audio/playback-audio-runtime.test.ts apps/web/src/features/playback/playback-handoff-policy.test.ts apps/web/src/features/playback/usePlaybackSessionRuntime.test.tsx
```

建议提交：`feat(playback): add bounded album gapless handoff`

## Task 5：把 Audio Graph 所有权移入 Runtime

**Files：**

- Modify: `apps/web/src/audio/playback-audio-runtime.ts`
- Modify: `apps/web/src/audio/playback-audio-runtime.test.ts`
- Modify: `apps/web/src/visual/runtime/create-legacy-visual-composition.ts`
- Modify: `apps/web/src/visual/runtime/create-legacy-visual-composition.test.ts`
- Modify: `apps/web/src/visual/VisualEngineHost.tsx`
- Modify: `apps/web/src/visual/VisualEngineHost.test.tsx`
- Modify: `apps/web/src/visual/useVisualEngine.ts`
- Modify: `apps/web/src/visual/useVisualEngine.test.ts`

### 5.1 RED：共享 mix 与只读 frame source

- [x] 每 deck 每 live AudioContext 最多一个 `MediaElementAudioSourceNode`。
- [x] A/B source 分别经 deck gain 进入 shared mix；crossfade 时 analyser 读取 combined signal。
- [x] visual consumer 只能读取 frame bytes/clock，不能获得 context/source/gain/connect/disconnect。
- [x] visual remount/ref change 不断开 committed 或 retiring deck。
- [x] dispose 后 source/analyser/gain/destination 连接和 frame source 全部归零/失效。

### 5.2 GREEN：迁移 ownership

- [x] Runtime graph：A source → A gain、B source → B gain、shared mix → main/beat analyser → master/destination。
- [x] 删除 visual composition 的 `createMediaElementSource` ownership；保留现有 FFT/smoothing 和 frame shape parity。
- [x] shelf/UI SFX 不通过暴露 AudioContext 破坏 visual read-only invariant；若需路由，使用窄 runtime action。

验证：

```powershell
bun test --parallel=1 apps/web/src/audio/playback-audio-runtime.test.ts apps/web/src/visual/runtime/create-legacy-visual-composition.test.ts apps/web/src/visual/VisualEngineHost.test.tsx apps/web/src/visual/useVisualEngine.test.ts
bun test --parallel=1 scripts/architecture/visual-runtime-boundary.test.ts
```

建议提交：`refactor(audio): centralize playback graph ownership`

## Task 6：Stall 与 Audio Graph 有界恢复

**Files：**

- Modify: `apps/web/src/audio/playback-audio-runtime.ts`
- Modify: `apps/web/src/audio/playback-audio-runtime.test.ts`
- Modify: `apps/web/src/features/playback/playback-session-coordinator.ts`
- Modify: `apps/web/src/features/playback/playback-session-coordinator.test.ts`
- Modify: `apps/web/src/features/playback/usePlaybackSessionRuntime.ts`
- Modify: `apps/web/src/features/playback/usePlaybackSessionRuntime.test.tsx`

### 6.1 RED：fake-clock recovery matrix

- [x] play deadline exactly 9000ms；canplay-ready retry 最多一次。
- [x] stall probes exactly 1600/3600ms；进度恢复后不刷新。
- [x] committed、remote、non-trial、non-local 且真实无进度时每 lineage 最多一次 fresh URL。
- [x] stale/pending/paused/ended/local/trial 不刷新。
- [x] graph probes exactly 720/1600/2800ms。
- [x] frozen clock 不重建 Graph；clock advancing 且连续两个 signal `<=0.0025` 才重建。
- [x] 每 owner generation 最多一次 graph reconnect/replacement。
- [x] 新 intent/dispose 清所有 deadline/listener/probe，旧 callback 无效。

### 6.2 GREEN：恢复 policy 接线

- [x] Runtime 只发布 typed recovery request/diagnostic；是否 fresh URL 仍由 coordinator 和现有 Playback Port 决定。
- [x] reuse existing media-error/URL-age/long-pause/quality rules，不复制 Provider fallback。
- [x] exhaustion 进入 existing failed/error/toast，不无限 loading。

验证：

```powershell
bun test --parallel=1 apps/web/src/audio/playback-audio-runtime.test.ts apps/web/src/features/playback/playback-session-coordinator.test.ts apps/web/src/features/playback/usePlaybackSessionRuntime.test.tsx
```

建议提交：`fix(playback): bound stall and audio graph recovery`

## Task 7：输出路由与设置 UI

**Files：**

- Create: `apps/web/src/features/playback/PlaybackAudioSettings.tsx`
- Create: `apps/web/src/features/playback/PlaybackAudioSettings.test.tsx`
- Create: `apps/web/src/features/playback/usePlaybackAudioSettings.ts`
- Create: `apps/web/src/features/playback/usePlaybackAudioSettings.test.tsx`
- Modify: `apps/web/src/audio/playback-audio-runtime.ts`
- Modify: `apps/web/src/audio/playback-audio-runtime.test.ts`
- Modify: existing Playback/Settings Surface and styles
- Modify: `apps/web/src/features/playback/PlaybackSurface.tsx`
- Modify: `apps/web/src/features/playback/PlaybackSurface.test.tsx`

### 7.1 RED：routing core

- [x] primary default/explicit sink、NotFound fallback 与 unsupported 状态。
- [x] mirrors 去重、排除 primary、最多四个；第五个不创建资源。
- [x] mirror interval 只有一个、周期 2200ms；drift `<=0.22s` 不 seek，`>0.22s` 才纠偏。
- [x] bridge enabled 把 virtual device 归一为 primary，且不创建 capture stream。
- [x] devicechange：primary missing → default；bridge missing → disabled/default；mirror missing → release runtime element and recoverable unavailable。
- [x] no mirrors/default primary/bridge disabled/panel closed 时 mirror Audio、interval、额外 poll 全为 0。
- [x] owner/source/pause/volume/rate change 同步 mirrors，stale mirror callback 不影响 owner。

### 7.2 GREEN：typed view model 与 UI

- [x] `listOutputDevices()` 只返回 immutable normalized outputs。
- [x] panel 展示 primary、mirror runtime state、Virtual Output Bridge、fade/gapless/crossfade 与 refresh。
- [x] label 为空时稳定 fallback；unsupported/unavailable 不伪造 connected。
- [x] preference commit 只走 `PreferencesRepository`，不新增 direct localStorage。

验证：

```powershell
bun test --parallel=1 apps/web/src/audio/playback-audio-runtime.test.ts apps/web/src/features/playback/PlaybackAudioSettings.test.tsx apps/web/src/features/playback/usePlaybackAudioSettings.test.tsx apps/web/src/features/playback/PlaybackSurface.test.tsx apps/web/src/preferences
```

建议提交：`feat(playback): add typed audio output routing`

## Task 8：Diagnostics、architecture 和 deterministic budget gate

**Files：**

- Create: `scripts/architecture/playback-audio-owner-boundary.test.ts`
- Create: `scripts/perf/m2-playback-budget.test.ts`
- Modify: `scripts/perf/m8-budget.mjs` 或建立独立 `perf:m2-playback` script（二选一，禁止重复跑 full suite）
- Modify: `package.json`

### 8.1 Architecture guard

- [x] 只有 `PlaybackAudioRuntime` 可创建 playback `AudioContext`、`MediaElementAudioSourceNode`、A/B deck 与 mirror elements。
- [x] visual Module 只能消费 `ReadonlyAudioFrameSource`，禁止 `createMediaElementSource`、deck mutation 与 sink mutation。
- [x] Feature/session/App 禁止直接 `setSinkId`、`enumerateDevices`、Graph connect/disconnect 或 mirror timer。
- [x] App 只装配 Runtime/Surface，不拥有 playback timers/effects。
- [x] M9 API freeze guard 继续固定 Sidecar/shared/client/supervisor/build/Cargo/conf blobs。

### 8.2 Deterministic budget

- [x] fake clock 证明：deck=2、committed≤1、pending≤1、handoff≤1、mirror≤4。
- [x] ready retry≤1、stall probes≤2/load、fresh URL≤1/lineage、graph probes≤3/owner、graph rebuild≤1/owner、audibility probes≤3/owner。
- [x] mirror interval≤1；disabled=0；dispose 后所有 current resources/timers/listeners=0。
- [x] `diagnostics()` immutable/serializable，不泄漏 DOM、AudioNode 或完整未裁剪 URL。

验证：

```powershell
bun test --parallel=1 scripts/architecture/playback-audio-owner-boundary.test.ts scripts/perf/m2-playback-budget.test.ts scripts/architecture/m9-api-readiness-boundary.test.ts
```

建议提交：`test(playback): enforce audio ownership budgets`

## Task 9：Parity 文档与全量收口

**Files：**

- Modify: `docs/parity/capability-matrix.md`
- Modify: `docs/parity/app-extraction-map.md`
- Modify: `docs/parity/upstream-source-map.md`
- Modify: umbrella convergence design M2 status
- Modify: 本 design/plan 的收口记录

- [x] 将六个 playback 项按真实实现与证据更新；只有所有代码目标完成才标 `implemented`。
- [x] 真实 WebView2 output、听感、睡眠/后台与 30–60 分钟 soak 标 `Field Validation Pending (non-blocking)`。
- [x] 明确该状态只阻止 `Field Validated / Release Verified`，不阻止 Code Complete。
- [x] 明确 Bun Sidecar 仍为生产 Adapter，未嵌入 Rust API。

### 9.1 Full Web/Bun gate

```powershell
bun test --parallel=1 packages/shared packages/visual-engine sidecars/api apps/web scripts/ci scripts/architecture scripts/perf
bun run typecheck
bun run web:build
bun run perf:budget
git diff --check
```

### 9.2 Rust gate

```powershell
cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --all -- --check
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets --all-features --locked
cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets --all-features --locked -- -D warnings
```

### 9.3 Frozen-line audit

```powershell
bun test --parallel=1 scripts/architecture/m9-api-readiness-boundary.test.ts
git diff --name-only 92a6447 -- sidecars/api packages/shared apps/web/src/api/sidecar-client.ts apps/desktop/src-tauri/src/sidecar.rs apps/desktop/scripts/build-sidecar-binary.mjs apps/desktop/src-tauri/build.rs apps/desktop/src-tauri/Cargo.toml apps/desktop/src-tauri/Cargo.lock apps/desktop/src-tauri/tauri.conf.json
```

Expected：第二条命令无输出。

建议提交：`docs: complete M2 playback parity evidence`

## Completion checkpoint

- [x] PlaybackAudioRuntime 是唯一 Audio/deck/Graph/fade/probe/routing owner。
- [x] pending 不发布 application media clock；committed owner exact 且唯一。
- [x] A/B handoff、gapless/crossfade/fade 有成功、失败、stale、取消和 dispose tests。
- [x] Visual 只读 frame source，切换时不提前断开 outgoing。
- [x] play/stall/Graph/audibility recovery 预算确定且可取消。
- [x] primary + ≤4 mirrors + Virtual Output Bridge 有 typed preference 与 device recovery。
- [x] disabled routing path 没有额外常驻 Audio/timer/poll。
- [x] Sidecar/shared/client/supervisor/build/Cargo/conf freeze 无 diff。
- [x] 自动门禁全绿；未完成实机项明确为 non-blocking field validation。

最终证据：播放域聚焦 `232 passed`；完整 Bun `2370 passed`；Rust `299 passed`；typecheck、Web build、M8/M2 performance budgets、fmt、clippy、freeze audit 与 `git diff --check` 全绿。真实 WebView2、设备、听感、睡眠恢复与 Windows soak 仍为 `Field Validation Pending (non-blocking)`。Bun Sidecar 继续作为 `legacy-frozen` production Adapter，Rust `MineRadio-api` 未嵌入。
